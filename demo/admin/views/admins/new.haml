%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:admins, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:admins, :new)

.tabs-content
  = form_for :admin, url(:admins, :create), :class => 'form-horizontal' do |f|
    =partial 'admins/form', :locals => { :f => f }
