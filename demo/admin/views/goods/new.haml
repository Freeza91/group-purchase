%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:goods, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:goods, :new)

.tabs-content
  = form_for :good, url(:goods, :create), :class => 'form-horizontal' do |f|
    =partial 'goods/form', :locals => { :f => f }
